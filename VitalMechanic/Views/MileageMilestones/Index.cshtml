@model IEnumerable<VitalMechanic.Models.MileageMilestone>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarMileage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Milestones)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CarMileage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Milestones)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Mileageid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Mileageid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Mileageid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
